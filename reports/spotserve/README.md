# SpotServe Reports

Generated benchmark reports should stay out of git. Keep durable notes or final
tables here only when they are small enough to review.

The default generated outputs live under:

```text
results/spotserve/<run_id>/
```
